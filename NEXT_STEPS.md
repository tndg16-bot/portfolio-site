# 次回の作業計画: ニュースレター機能の実装

## ✅ 完了した作業
- ブログ詳細ページのUI修正（戻るリンク、タグ、カテゴリ、日付）
- OG画像URLのローカル/本番対応修正
- コメント機能(Giscus)の有効化と動作確認

## 🚀 次に取り組むタスク (#13)
**ニュースレター登録フォームの実装とバックエンドAPIの構築**

## 📋 事前準備（ユーザー様へのお願い）
本格的な配信を行う場合、メール配信サービスのAPIキーが必要です。
- **候補**: SendGrid, ConvertKit, Resend など
- **現状**: まだ決まっていない場合は、まずは「登録されたメールアドレスをログ出力するだけ」や「CSV保存」などのモック実装から始め、UI/UXを先に完成させるのがおすすめです。

## 🛠️ 実装ステップ概要
1. **コンポーネント確認**: `src/components/NewsletterForm.tsx` の現状確認と改修。
2. **API作成**: `src/app/api/newsletter/route.ts` を作成し、POSTリクエストを処理するロジックを実装。
3. **バリデーション**: `zod` 等を使ったメールアドレスの形式チェック。
4. **フィードバックUI**: 送信成功/失敗のトースト通知やメッセージ表示。

## 🤖 次回セッション用プロンプト案
次回、ターミナルでAIエージェントを起動した際、以下のテキストをそのまま貼り付けて指示してください：

```text
前回の続きで、`Apps/Main/portfolio` プロジェクトのニュースレター機能の実装 (#13) を開始してください。
まずは `src/components/NewsletterForm.tsx` の内容を確認し、APIルート `/api/newsletter` の作成から進めてください。
配信サービスはまだ未定なので、まずはAPIがリクエストを受け取れる状態にすることを目標にします。
```
